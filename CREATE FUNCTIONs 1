-- Start by creating the function
CREATE FUNCTION dbo.ConvertMileToKm (@Miles numeric(18,2))
-- Don't forget to specify the return type
RETURNS numeric(18,2)
AS
BEGIN
    -- Your conversion logic goes here
    RETURN (SELECT @Miles * 1.609)
END;
